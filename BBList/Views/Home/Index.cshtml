@{
    ViewData["Title"] = "Home Page";
}

<div class="flex flex-1 flex-col items-center justify-center">
    <div class="flex min-h-[300px] flex-col gap-3">
        <div class="">
            <h1 class="font-rokkit text-center text-4xl font-bold text-red-600">BBList</h1>
            <p>Plan and build your Blood Bowl roster with BBList!</p>
        </div>
        <div class="flex justify-center gap-3">
            <a variant="Outline" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
            <a variant="Outline" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </div>
    </div>

    <card>
        <card-content>
            <card-header>
                <card-title>
                    <h2>Card Heading</h2>
                </card-title>
                <card-description>
                    <p>Card Description</p>
                </card-description>
            </card-header>
            <card-content>
                Content
            </card-content>
        </card-content>
    </card>
</div>
